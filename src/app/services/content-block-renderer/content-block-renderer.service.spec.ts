import { TestBed } from '@angular/core/testing';

import { ContentBlockRendererService } from './content-block-renderer.service';

describe('ContentBlockRendererService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    // const service: ContentBlockRendererService = TestBed.get(
    //   ContentBlockRendererService
    // );
    expect(true).toBeTruthy();
  });
});
