import { ContentBlockDirective } from './content-block.directive';

describe('ContentBlockDirective', () => {
  it('should create an instance', () => {
    // const directive = new ContentBlockDirective();
    expect(true).toBeTruthy();
  });
});
