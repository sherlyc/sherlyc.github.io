version: "3.7"
services:
  expfrontend:
    image: ${DOCKER_URL}
    environment:
      SPADE_VERSION: ${SPADE_VERSION}
    ports:
      - "4000:4000"
  puppet:
    build: .
    environment:
      URL: http://expfrontend:4000/
    depends_on:
      - expfrontend
