describe("News six handler", () => {
  it("should work log and throw error when not enough articles are available", () => {
    expect(1).toBe(1);
  });

  it("should retrieve articles by source id and limit", () => {
    expect(1).toBe(1);
  });

  it("should provide correct input to next handler", () => {
    expect(1).toBe(1);
  });
});
