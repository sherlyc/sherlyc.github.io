import { IHttpError } from './interfaces/HttpError';

// export const toHttpError = (error: Error, statusCode: string): IHttpError => {
//   const httpError: IHttpError = error;
//   httpError.statusCode = statusCode;
//   return httpError;
// };
