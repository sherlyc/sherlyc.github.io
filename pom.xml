<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd" xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>nz.stuff.stable.yokohama</groupId>
    <artifactId>yokohama-parent</artifactId>
    <version>1.1</version>
  </parent>

  <groupId>nz.stuff.stable.experience</groupId>
  <artifactId>stuff-experience-frontend</artifactId>
  <version>0.999-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>${project.artifactId}</name>
  <description>Kubernetes configuration to deploy this application</description>

  <properties>
    <microserviceGroup>experience</microserviceGroup>
    <tile-deploy.nodeVersionRange>[8,9)</tile-deploy.nodeVersionRange>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.6.0</version>
        <executions>
          <execution>
            <id>npm-install</id>
            <goals>
              <goal>exec</goal>
            </goals>
            <phase>generate-resources</phase>
            <configuration>
              <workingDirectory>${basedir}</workingDirectory>
              <executable>npm</executable>
              <arguments>
                <argument>ci</argument>
              </arguments>
            </configuration>
          </execution>

          <execution>
            <id>npm-run-build</id>
            <goals>
              <goal>exec</goal>
            </goals>
            <phase>compile</phase>
            <configuration>
              <workingDirectory>${basedir}</workingDirectory>
              <executable>npm</executable>
              <arguments>
                <argument>run</argument>
                <argument>build</argument>
              </arguments>
            </configuration>
          </execution>

        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.0.2</version>
        <executions>
          <execution>
            <id>copy-ng-resources</id>
            <phase>package</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/docker/resources/srv/${project.artifactId}/dist</outputDirectory>
              <resources>
                <resource>
                  <directory>dist</directory>
                  <filtering>false</filtering>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>io.repaint.maven</groupId>
        <artifactId>tiles-maven-plugin</artifactId>
        <version>2.11</version>
        <extensions>true</extensions>
        <configuration>
          <tiles>
            <tile>nz.stuff.stable.yokohama:yokohama-tile-distribution:[1,2)</tile>
            <tile>io.practiv.stable.tile:practiv-tile-buildinfo:[2,3)</tile>
            <tile>io.practiv.stable.tile:practiv-tile-release:[2,3)</tile>
            <tile>io.practiv.stable.tile:practiv-tile-kubernetes-application:[2,3)</tile>
            <tile>io.practiv.stable.tile:practiv-tile-deploy-node-image:[1.5,2)</tile>
          </tiles>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
